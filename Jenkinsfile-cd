pipeline {
    agent any
    stages {
        stage('Publish over ssh plugin in pipeline') {
                
                        sshPublisher(
                            publishers: [
                                sshPublisherDesc(
                                    configName: Frontend, 
                                    transfers: [
                                        sshTransfer(
                                            cleanRemote: false, 
                                            excludes: '', 
                                            execCommand: 'cd microservice-app-example/ ; sudo git pull',
                                        )
                                    ]
                                )
                            ]
                        )
                    }
                }
            }

